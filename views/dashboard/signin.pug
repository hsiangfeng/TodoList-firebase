<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Document
    link(rel="stylesheet", href="/css/all.css")
  body
    .text-center.from-height.d-flex
      form.form-signin(action="/auth/admin/signin", method="post")
        h1.h3.mb-3.font-weight-normal= title
        label.sr-only(for='inputEmail') Email address
        input#inputEmail.form-control(type='email', placeholder='Email address', required='', autofocus='', name="email")
        label.sr-only(for='inputPassword') Password
        input#inputPassword.form-control(type='password', placeholder='Password', required='', name="password")
        .checkbox.mb-3
          label
            input(type='checkbox', value='remember-me' required)
            |  你已經明白這是一個作品請打勾
        if hesInfo
          .alert.alert-danger
            h6= error
        button.btn.btn-lg.btn-primary.btn-block(type='submit') Sign in
        p.mt-5.mb-3.text-muted © 2017-2018